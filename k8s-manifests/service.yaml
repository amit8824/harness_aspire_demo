apiVersion: v1
kind: Service
metadata:
  name: harness-aspire-demo-svc
spec:
  type: NodePort
  selector:
    app: harness-aspire-demo
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
